name: テストを自動実行

on:
  push:           # コードがプッシュされたら
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest   # 実行環境
    steps:
      - uses: actions/checkout@v4   # コードを取得
      - run: npm test               # テスト実行
